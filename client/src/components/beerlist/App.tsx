import * as React from "react";
import BeerListContainer from "../../containers/BeerListContainer";

const BeerListApp: React.SFC = () => (
  <div>
      <BeerListContainer />
  </div>
);

export default BeerListApp;
